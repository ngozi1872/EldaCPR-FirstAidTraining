@page
@model EldaCPRFirstAidTraining.Pages.TrainingSchedule.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrainingSchedule[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainingSchedule[0].EndDate)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.TrainingSchedule[0].StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainingSchedule[0].EndTime)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.TrainingSchedule[0].Training)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TrainingSchedule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.StartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTime)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Training.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
